- name: local-step1
  template: local-step
  arguments:
    parameters:
      - name: message
        value: step-1
- name: local-step2
  templateRef:
    name: common-toolkit
    template: github-notify
    clusterScope: true
  arguments:
    parameters:
      - name: message
        value: step-2
  dependencies:
    - local-step1